# Docker Compose — copy to .env and fill in values before running docker compose up
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=change_me

# Production only
FRONTEND_URL=
VITE_API_URL=
GOOGLE_SERVICE_ACCOUNT_PATH=/path/to/cyf-service-account.json
